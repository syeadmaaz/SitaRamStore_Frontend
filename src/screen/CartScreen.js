import React, {useState, useEffect} from "react";
import {
    RefreshControl,
    StyleSheet,
    View,
    Text,
    TouchableOpacity,
    Image,
    SafeAreaView,
    FlatList,
    StatusBar,
  } from "react-native";

const CartScreen = () => {
    return (
        <SafeAreaView>
            <Text>CART SCREEN</Text>
        </SafeAreaView>
    )
}

export default CartScreen